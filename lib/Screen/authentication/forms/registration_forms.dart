import 'package:flutter/material.dart';

class registeration_forms extends StatelessWidget {
  const registeration_forms({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}